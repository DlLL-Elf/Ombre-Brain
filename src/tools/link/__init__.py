"""
========================================
tools/link/__init__.py — link 补线工具入口
========================================

link 是「补线」：手动声明一条跨时间的边（3.5.0）。
时间线给「顺序」（然后呢），补线给「叙事」（所以呢）——跨越时间的经历连续。

对外暴露：link(bucket_id, target_bucket_id, relation_type, label) → str
========================================
"""

from .core import link  # noqa: F401
